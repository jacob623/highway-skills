# library/templates/

Markdown templates shared across more than one skill's output shape. A template's placeholder
text is not itself a rule statement — it is the shape a downstream skill's output takes — so
templates are checked against a smaller subset of constitution rules than knowledge or
governance files (see [validate-library.sh](../../tools/validate-library.sh)).

This README documents the directory's purpose only; it is not itself a template.
