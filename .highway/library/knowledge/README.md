# library/knowledge/

Markdown knowledge files shared across more than one skill: reference material, background, and
prose a skill's body can point to instead of duplicating. Knowledge files are checked against
the same rule-content checks a skill's body gets (see
[validate-library.sh](../../tools/validate-library.sh)).

This README documents the directory's purpose only; it is not itself a knowledge file.
