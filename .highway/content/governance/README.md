# content/governance/

Markdown governance files shared across more than one skill: policy statements a skill's body
can point to instead of duplicating. Governance files are checked against the same
rule-content checks a skill's body gets (see
[validate-content.sh](../../tools/validate-content.sh)).

This README documents the directory's purpose only; it is not itself a governance file.
