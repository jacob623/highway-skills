# Runtime hygiene fixtures

These strings are disposable seeded probes only. They are never copied into shipped runtime
contracts. The hygiene test uses them to prove that known feature, requirement, branch, plan, and
task identifiers remain detectable while stable P/X governance references remain allowed.
